import rcadeserver.*;

class BootStrap {

	def init = { servletContext ->
		/*
		def zach = new Player(
				playerID:13654,
				name:"Zach McGaughey"
				)
		zach.save()
		if(zach.hasErrors()) {
			println zach.errors
		}
		
		def pacman = new Game(
			romName:"pacman",
			gameName:"Pac Man"
			)
		pacman.save()
		if(pacman.hasErrors()){
			println pacman.errors
		}
		
		def testScore = new Score(
			player:zach,
			game:pacman,
			score: 42000,
			cabinetID:0
			)
		testScore.save()
		if(testScore.hasErrors()){
			println testScore.errors
		}
		*/
	}
	def destroy = {
	}
}
